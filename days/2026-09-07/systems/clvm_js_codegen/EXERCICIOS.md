# Exercícios — clvm_js_codegen

## Médio

- **CLVM-JS-LET-01:** lowering de `let`/`const` (e use o mesmo padrão em assign).
- **CLVM-JS-WHILE-01:** lowering de `while`.

## Difícil

- **CLVM-JS-CALL-01:** `CALL` + prólogo de parâmetros + chamadas em expressões.
