import {Worker} from "node:worker_threads";
export async function runTransfer(){
  // TODO [D6-NODE-WORKER]: crie worker module.
  // TODO [D6-NODE-TRANSFER]: preencha e transfira ArrayBuffer.
  return {sum:0,length:0,detached:false};
}
