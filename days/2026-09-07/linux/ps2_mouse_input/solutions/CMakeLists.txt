cmake_minimum_required(VERSION 3.16)
project(ps2_mouse C)
enable_testing()
add_executable(ps2_mouse_test ps2_mouse.c test_ps2_mouse.c)
target_compile_definitions(ps2_mouse_test PRIVATE FIXTURES_DIR="${CMAKE_CURRENT_SOURCE_DIR}/fixtures")
if(MSVC)
  target_compile_options(ps2_mouse_test PRIVATE /UNDEBUG)
endif()
add_test(NAME ps2_mouse_test COMMAND ps2_mouse_test)
