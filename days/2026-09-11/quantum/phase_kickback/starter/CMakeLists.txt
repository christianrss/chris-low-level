cmake_minimum_required(VERSION 3.16)
project(phase CXX)
set(CMAKE_CXX_STANDARD 17)
enable_testing()
add_executable(phase_test phase.cpp test_phase.cpp)
if(MSVC)
  target_compile_options(phase_test PRIVATE /W3 /EHsc)
endif()
add_test(NAME phase_test COMMAND phase_test)
