cmake_minimum_required(VERSION 3.16)
project(bump CXX)
set(CMAKE_CXX_STANDARD 17)
enable_testing()
add_executable(bump_test bump.cpp test_bump.cpp)
if(MSVC)
  target_compile_options(bump_test PRIVATE /W3 /EHsc)
endif()
add_test(NAME bump_test COMMAND bump_test)
