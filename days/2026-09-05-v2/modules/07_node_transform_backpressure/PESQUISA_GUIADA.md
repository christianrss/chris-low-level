# Pesquisa guiada

1. Explique `highWaterMark` em Writable.
2. Pesquise por que `StringDecoder` existe.
3. Diferencie evento `finish` de `end`.
4. Desenhe o fluxo produtor -> buffer -> consumidor quando `write()` retorna false.