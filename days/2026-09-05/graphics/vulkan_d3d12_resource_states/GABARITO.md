# Gabarito — Graphics — Estados Vulkan/D3D12

Respostas esperadas (consulte `solutions/` para código completo).

1. CopyDst→ShaderRead→RenderTarget→Present permitido.
2. Present→RenderTarget permitido (loop).
3. CopyDst direto para Present → false.
4. to_vulkan/to_d3d12 retornam strings canônicas.
