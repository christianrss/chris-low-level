# Pesquisa guiada — clvm_js_codegen

1. Qual a ordem da pilha em `STORE` (value vs addr)?
2. Por que o prólogo de params usa `reversed(fn.params)`?
3. O que acontece se dois `while` reutilizarem o mesmo label literal `"loop"`?

Checkpoint: responda sem olhar a resolução.
