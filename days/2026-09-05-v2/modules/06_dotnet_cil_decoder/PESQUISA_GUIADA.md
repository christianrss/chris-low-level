# Pesquisa guiada

1. Diferencie CIL, CLR e JIT em três frases.
2. Pesquise o efeito de pilha de `ldc.i4.s`, `add` e `ret`.
3. Explique por que um operand `0xFF` deve virar `-1` em `sbyte`.
4. Compare bytecode stack-based com uma IR de registradores.