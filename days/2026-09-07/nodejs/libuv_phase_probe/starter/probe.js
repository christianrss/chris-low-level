export async function probe(){ // TODO [D5-NODE-PROBE]: capture ordem parcial.
 return [];
}
export async function boundedNextTick(limit){ // TODO [D5-NODE-BOUNDED]: recursão limitada.
 return 0;
}
export function yieldImmediate(){ // TODO [D5-NODE-YIELD]: Promise via setImmediate.
 return Promise.resolve();
}
