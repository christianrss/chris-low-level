# Exercícios

- **Fácil - LINUX-PKG-PARSE-01:** implemente `load_manifest()` em `starter/chris_pkg.py`, validando JSON, `name`, `version` e `files`.
- **Médio - LINUX-PKG-PARSE-01:** rejeite caminho absoluto e componente `..`.
- **Difícil - LINUX-PKG-INSTALL-02:** implemente `install_package()` sem tocar no banco antes de validar as fontes do payload.
- **Médio - LINUX-ROOTFS-BUILD-03:** complete `starter/build_rootfs.sh` e prove idempotência executando o teste duas vezes.
- **Reflexão:** descreva como adicionar hashes por arquivo sem quebrar a simplicidade do formato.