cmake_minimum_required(VERSION 3.16)
project(coff C)
enable_testing()
if(MSVC)
  enable_language(ASM_MASM)
  add_executable(coff_test test_main.c coff_sym.asm)
else()
  enable_language(ASM)
  add_executable(coff_test test_main.c coff_sym.S)
endif()
add_test(NAME coff_test COMMAND coff_test)
