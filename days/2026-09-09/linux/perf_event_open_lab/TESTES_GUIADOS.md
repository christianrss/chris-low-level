# Testes guiados

## Caso 1: `LX-PERF-OPEN-01`

fd > 0.

## Caso 2: `LX-PERF-READ-02`

read == 42.

## Caso 3: `LX-PERF-CLOSE-03`

close True e fd removido.

## Identificadores

- `LX-PERF-OPEN-01` — exercido pelo caso acima; o assert usa o valor da TEORIA.

- `LX-PERF-READ-02` — exercido pelo caso acima; o assert usa o valor da TEORIA.

- `LX-PERF-CLOSE-03` — exercido pelo caso acima; o assert usa o valor da TEORIA.
