# Pesquisa guiada

1. Explique a diferença entre `miscdevice` e registrar major/minor manualmente.
2. Pesquise por que código de kernel não pode usar `printf`/libc normal.
3. Liste três bugs de lifecycle comuns: use-after-free, double release e resource leak.
4. Em `chris_char.c`, identifique exatamente o recurso adquirido e o recurso liberado.