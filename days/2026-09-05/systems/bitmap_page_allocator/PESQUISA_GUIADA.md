# Pesquisa guiada — Systems: bitmap page allocator

Pesquise “Linux physical page allocator buddy allocator” apenas para comparar: por que o kernel usa ordens/blocos em vez de first-fit bit-a-bit? Pesquise `struct page` e responda por que um bitmap sozinho não carrega todos os metadados necessários.

## Regra
Use as fontes para **entender e validar**. Não copie uma implementação pronta para preencher o starter. Registre em poucas linhas o que aprendeu e qual decisão do exercício a fonte ajuda a justificar.
