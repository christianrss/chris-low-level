# Exercícios — descriptor_binding_model

1. Complete `GFX-DESC-LAYOUT`, `GFX-DESC-BIND`, `GFX-DESC-SAMPLE`.
2. Rode `ctest` no starter.
3. Observe a demo em solutions: o draw só chama `sample(i)` — nunca lê literais de cor hardcoded no loop de painéis.
4. Extra: quarto slot com cinza pulsante.
