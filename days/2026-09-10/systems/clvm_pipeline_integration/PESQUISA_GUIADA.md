# Pesquisa guiada — Pipeline CLVM: disasm + peephole + verify

Responda no papel com valores verificáveis neste lab (não ensaios vagos).

1. Por que PUSH 0 ADD some?
2. Tamanho do PUSH?
3. Underflow de stack?
4. Ordem disasm→peep→verify?
5. Relação com rust cross_verify?

## Fontes

- Dia 08 systems/clvm_peephole_opt
- Dia 10 rust/cross_verify_clvm
