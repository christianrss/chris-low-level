# Testes guiados

A fixture verifica texto normal, mudança para vermelho, reset e cursor `2;3H` -> `(1,2)`. A solution imprime `OK ANSI`. Regressões futuras: parâmetros vazios, sequência incompleta e texto Unicode.