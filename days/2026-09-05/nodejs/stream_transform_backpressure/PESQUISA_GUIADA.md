# Pesquisa guiada — Node.js: Transform stream + backpressure observável

Leia Node.js docs de Stream, `writable.write()` e `StringDecoder`. Perguntas: o que significa `false` em `write()`? Por que ignorar isso pode elevar memória? Por que `Buffer.toString()` por chunk pode corromper UTF-8 dividido?

## Regra
Use as fontes para **entender e validar**. Não copie uma implementação pronta para preencher o starter. Registre em poucas linhas o que aprendeu e qual decisão do exercício a fonte ajuda a justificar.
