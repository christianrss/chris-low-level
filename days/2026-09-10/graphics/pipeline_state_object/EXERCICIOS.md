# Exercícios — pipeline_state_object

1. Implemente `GFX-PSO-CREATE`, `GFX-PSO-BIND`, `GFX-PSO-CYCLE` em `starter/core/pso.cpp`.
2. Confirme com `ctest` no build do starter.
3. Estude `solutions/software_win32` e `solutions/opengl_win32` para ver como o PSO ativo escolhe fill vs wire.
4. Extra: adicione um quarto preset (quad) sem quebrar o ciclo de 3 nos testes.
