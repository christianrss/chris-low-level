# Pesquisa guiada

1. Diferencie endereço virtual (`e_entry`) de file offset.
2. Pesquise os valores comuns de `e_machine` para x86-64 e AArch64.
3. Explique por que endianness precisa ser validada antes de `struct.unpack`.
4. Compare ELF e PE apenas em alto nível: magic/header/sections/entry point.