# Pesquisa guiada — pipeline_state_object

1. O que é um Pipeline State Object em APIs modernas (D3D12/Vulkan/Metal) e por que se “congela” estado?
2. Diferença entre `GL_TRIANGLES` e `GL_LINE_LOOP` no legado OpenGL.
3. Como `StretchDIBits` apresenta um framebuffer CPU no Win32.
4. Por que o lab separa `core/` (testável) dos backends de janela.
