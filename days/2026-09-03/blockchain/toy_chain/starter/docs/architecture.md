# Architecture

Each block hashes canonical JSON containing its previous block hash and a Merkle root of transaction strings. Mining increments a nonce until the hexadecimal SHA-256 digest has a toy zero-prefix. This is a local learning model, not a cryptocurrency implementation.
