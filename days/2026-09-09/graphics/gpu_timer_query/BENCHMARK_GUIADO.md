# Benchmark guiado — GPU timer query (simulação headless)

## Hipótese

A métrica `ms do lap draw (3 corridas)` permanece estável (±20%) em 3 corridas no mesmo hardware.

## Método

```powershell
python days/2026-09-09/graphics/gpu_timer_query/solutions/test_gpu_timer.py
```

Repita 3 vezes; anote tempo de parede ou a métrica específica do lab.

## Resultados observados

não executado neste ambiente na geração do dia — registre aqui: ms do lap draw (3 corridas).
