# Benchmarks

Small checked-in results belong here only when they are useful for comparison and include environment metadata. Large profiler captures should not be committed; attach them to Releases or keep them locally.
