# Pesquisa guiada — async_hooks: fases e métricas (JavaScript)

Responda no papel com valores verificáveis neste lab (não ensaios vagos).

1. O que async_hooks.init observa?
2. Diferença before/after?
3. Por que enable é necessário?
4. Overhead de async_hooks em produção?
5. Como OTEL Node se relaciona?

## Fontes

- Node.js async_hooks docs
