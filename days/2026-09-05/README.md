# Day 03 — 2026-09-05

Day 03 continua os dois dias anteriores e inaugura formalmente as trilhas permanentes de **Linux distribution/kernel engineering** e **Vulkan/D3D12 explicit graphics**.

## Módulos
1. Linux distro — pacote próprio + rootfs reproduzível.
2. Linux kernel — lifecycle de char device + revisão de módulo real.
3. Systems — bitmap page allocator.
4. AI/ML Systems — matmul naive + tiled.
5. Red Team seguro — ELF64 entry-point inspector.
6. .NET/CLR — tiny CIL decoder (não executado: SDK .NET ausente).
7. Node.js — Transform stream + backpressure observável.
8. JavaScript VM — JZ/JMP e controle de fluxo.
9. Graphics — resource-state tracker com mapeamento Vulkan/D3D12 + debug shaders.
10. Linux terminal — ANSI/CSI parser preparando PTY/TTY.

## Honestidade de execução
Vulkan/D3D12 são ensinados hoje por um simulador portátil de estados + shaders fonte; nenhum backend real é declarado como executado. O módulo de kernel real é revisão de fonte; o modelo de lifecycle é o artefato executado. .NET não foi executado por ausência do SDK.
