# Testes guiados

O programa representa um `if`: condição zero deve pular para o ramo que empilha 20, e o JMP impede cair no outro ramo. O teste valida resultado final da stack. A solution imprime `OK js vm`. Regra de regressão futura: rejeitar target fora do programa.