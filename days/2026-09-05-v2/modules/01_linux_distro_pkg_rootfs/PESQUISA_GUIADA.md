# Pesquisa guiada

1. Pesquise o papel de `/var/lib` no Filesystem Hierarchy Standard e anote por que um banco de pacotes pertence ali.
2. Compare conceitualmente `manifest + payload` com um pacote `.deb`: quais ideias são parecidas e quais recursos profissionais ainda faltam?
3. Explique em 5 linhas a diferença entre idempotência e atomicidade.
4. Leia sobre path traversal e escreva dois exemplos de caminhos que o parser deve rejeitar.